# Configuration

Properties accepted by the connector:

| Property | Description |
|---|---|
| `exampleInstanceName` | Fully qualified instance name. |
| `exampleTargetPrincipal` | Service account to impersonate. |
| `exampleIpType` | `PRIVATE` (default) or `PUBLIC`. |
